package com.lowes.qa.selenium.components;

import com.lowes.qa.selenium.support.DriverScript;
import com.lowes.qa.selenium.support.ReusableLibrary;
import com.lowes.qa.selenium.support.ScriptHelper;


/**
 * Component Library template
 * @author Cognizant
 */
public class Template extends ReusableLibrary
{
	/**
	 * Constructor to initialize the component library
	 * @param scriptHelper The {@link ScriptHelper} object passed from the {@link DriverScript}
	 */
	public Template(ScriptHelper scriptHelper)
	{
		super(scriptHelper);
	}
	
	// Add your business component functions here, with 'public' access and 'void' return type
	// Other return types can also be used if necessary
}